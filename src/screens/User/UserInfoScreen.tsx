import styled from 'styled-components';

function UserInfoScreen() {
  return <Text>UserInfo</Text>;
}

const Text = styled.span`
  font-size: 2.4rem;
  font-weight: 600;
`;

export default UserInfoScreen;
